<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:RozpoznawanieMatwarzy.ViewModels"
             x:Class="RozpoznawanieMatwarzy.Views.StronaLogowania"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F5F5">
    
    <ContentPage.BindingContext>
        <viewmodels:ModelLogowania />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
            
            <!-- Logo / Ikona -->
            <Frame BackgroundColor="White" 
                   CornerRadius="80" 
                   WidthRequest="160" 
                   HeightRequest="160" 
                   Padding="0"
                   HorizontalOptions="Center"
                   HasShadow="True">
                <Label Text="🎭" 
                       FontSize="80" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"/>
            </Frame>

            <!-- Tytuł -->
            <Label Text="Rozpoznawanie Twarzy" 
                   FontSize="28" 
                   FontAttributes="Bold"
                   HorizontalOptions="Center" 
                   TextColor="#333333"
                   Margin="0,20,0,10"/>

            <Label Text="Zaloguj się aby kontynuować" 
                   FontSize="16" 
                   HorizontalOptions="Center" 
                   TextColor="#666666"
                   Margin="0,0,0,30"/>

            <!-- Formularz logowania -->
            <Frame BackgroundColor="White" 
                   CornerRadius="15" 
                   Padding="20" 
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    
                    <!-- Nazwa użytkownika -->
                    <Label Text="Nazwa użytkownika" 
                           FontSize="14" 
                           FontAttributes="Bold"
                           TextColor="#333333"/>
                    <Frame BorderColor="#E0E0E0" 
                           CornerRadius="10" 
                           Padding="0"
                           HasShadow="False">
                        <Entry Text="{Binding Username}" 
                               Placeholder="Wprowadź nazwę użytkownika"
                               FontSize="16"
                               Margin="10,5"/>
                    </Frame>

                    <!-- Hasło -->
                    <Label Text="Hasło" 
                           FontSize="14" 
                           FontAttributes="Bold"
                           TextColor="#333333"
                           Margin="0,10,0,0"/>
                    <Frame BorderColor="#E0E0E0" 
                           CornerRadius="10" 
                           Padding="0"
                           HasShadow="False">
                        <Entry Text="{Binding Password}" 
                               Placeholder="Wprowadź hasło"
                               IsPassword="True"
                               FontSize="16"
                               Margin="10,5"/>
                    </Frame>

                    <!-- Przycisk logowania -->
                    <Button Text="🔐 Zaloguj się" 
                            Command="{Binding ZalogujCommand}"
                            BackgroundColor="#667eea"
                            TextColor="White"
                            FontSize="18"
                            FontAttributes="Bold"
                            HeightRequest="55"
                            CornerRadius="10"
                            Margin="0,20,0,0"/>

                    <!-- Komunikat -->
                    <Label Text="{Binding Komunikat}" 
                           FontSize="14"
                           TextColor="{Binding KolorKomunikatu}"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           FontAttributes="Bold"
                           Margin="0,10,0,0"/>

                    <!-- Wskaźnik ładowania -->
                    <ActivityIndicator IsRunning="{Binding JestZajety}" 
                                     IsVisible="{Binding JestZajety}"
                                     Color="#667eea" 
                                     HeightRequest="40"/>

                </VerticalStackLayout>
            </Frame>

            <!-- Informacje dodatkowe -->
            <VerticalStackLayout Spacing="10" Margin="0,30,0,0">
                <Label Text="Domyślne dane logowania:" 
                       FontSize="12" 
                       HorizontalOptions="Center"
                       TextColor="#999999"/>
                <Label Text="Użytkownik: Admin" 
                       FontSize="12" 
                       HorizontalOptions="Center"
                       TextColor="#999999"
                       FontFamily="Courier"/>
                <Label Text="Hasło: Admin123" 
                       FontSize="12" 
                       HorizontalOptions="Center"
                       TextColor="#999999"
                       FontFamily="Courier"/>
            </VerticalStackLayout>

            <!-- Wersja -->
            <Label Text="v1.0.0" 
                   FontSize="12" 
                   HorizontalOptions="Center"
                   TextColor="#CCCCCC"
                   Margin="0,20,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
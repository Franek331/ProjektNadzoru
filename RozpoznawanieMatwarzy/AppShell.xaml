<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RozpoznawanieMatwarzy.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RozpoznawanieMatwarzy"
     xmlns:views="clr-namespace:RozpoznawanieMatwarzy.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="RozpoznawanieMatwarzy">



    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:StronaLogowania}"
        Route="LoginPage"
        FlyoutItemIsVisible="True" />

    <!-- Menu główne (widoczne po zalogowaniu) -->

    <FlyoutItem Title="📸 Rejestracja" Route="RegisterPage">
        <ShellContent ContentTemplate="{DataTemplate views:StronaRejestracji}" />
    </FlyoutItem>

    <FlyoutItem Title="🔍 Rozpoznawanie" Route="RecognizePage">
        <ShellContent ContentTemplate="{DataTemplate views:StronaRozpoznawania}" />
    </FlyoutItem>

    <FlyoutItem Title="📸 Raport" Route="RaportPage">
        <ShellContent ContentTemplate="{DataTemplate views:StronaRaportu}" />
    </FlyoutItem>

    <!-- Wylogowanie -->
    <MenuItem Text="🚪 Wyloguj" Clicked="OnWylogujClicked" />

    <!-- Stylowanie menu -->
    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid BackgroundColor="#764ba2" HeightRequest="150" Padding="20">
                <VerticalStackLayout VerticalOptions="Center">
                    <Label Text="🎭" FontSize="50" HorizontalOptions="Center" TextColor="Black"/>
                    <Label Text="Rozpoznawanie Twarzy" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           HorizontalOptions="Center" 
                           TextColor="Black"
                           Margin="0,10,0,0"/>
                    <Label x:Name="UsernameLabel"
                           Text="Zalogowano jako: Admin" 
                           FontSize="12" 
                           HorizontalOptions="Center" 
                           TextColor="Black"
                           Opacity="0.8"/>
                </VerticalStackLayout>
            </Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid Padding="15,10">
                <Label Text="{Binding Title}" 
                       FontSize="16" 
                       TextColor="Black"
                       VerticalOptions="Center"/>
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>
</Shell>
